# Web3Forms access key (https://web3forms.com). Public by design: it only identifies the
# destination inbox. Must keep the PUBLIC_ prefix so the contact form island can read it.
PUBLIC_WEB3FORMS_KEY=your-web3forms-access-key

# Optional: Google Search Console "HTML tag" verification token (the content="..." value).
PUBLIC_GOOGLE_SITE_VERIFICATION=
